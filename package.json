{
    "name": "youtube_shorts_normal_player",
    "version": "1.1.6",
    "license": "MIT",
    "scripts": {
        "dev": "web-ext run",
        "dev:chrome": "web-ext run -t chromium",
        "dev:mobile": "web-ext run --target=firefox-android",
        "build": "web-ext build -i TANK .gitignore .prettierignore .prettierrc .editorconfig yarn.lock package.json"
    },
    "devDependencies": {
        "web-ext": "^7.6.1"
    }
}
